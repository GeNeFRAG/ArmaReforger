{
  "name": "arma-mortar-calculator",
  "version": "2.7.0",
  "description": "Ballistic calculation engine for Arma Reforger mortar and MLRS weapon systems.",
  "main": "BallisticCalculator.js",
  "scripts": {
    "build:mobile": "bash scripts/build-mobile.sh",
    "sync": "npm run build:mobile && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios",
    "android:run": "npm run sync && npx cap run android",
    "ios:run": "npm run sync && npx cap run ios"
  },
  "keywords": ["arma-reforger", "mortar", "ballistic-calculator"],
  "author": "ArmaMortars.org",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/cli": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.0.2"
  }
}
